const BASE_URL = 'https://free-food-menus-api-production.up.railway.app'

const requests = {
    fetchBurger: `${BASE_URL}/burgers`,
    fetchBBQs: `${BASE_URL}/bbqs`,
    fetchBreads: `${BASE_URL}/breads`,
    fetchChocolates: `${BASE_URL}/chocolates`,
    fetchDrinks: `${BASE_URL}/drinks`
}